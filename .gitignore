node_modules/
dist/
output/
.env.local
.env
*.log
.DS_Store

# One-off debug scripts
debug-*.ts

# Big SFX library (downloaded by `npm run sfx:download` — too large to commit)
# Anchored to repo root so it does NOT match assets/sfx/ on case-insensitive FS.
/SFX/

# ── Media (videos / audio / generated images) — keep the repo code-only ──
# Rendered output (videos, voice, frames, staged images) — all generated
video/output/
podcast/output/
podcast/_runs/
# Source/generated media inside content input folders (grok images, footage, voice)
video/input/**/*.jpg
video/input/**/*.jpeg
video/input/**/*.png
video/input/**/*.webp
video/input/**/*.mp4
video/input/**/*.mp3
podcast/**/*.mp4
podcast/**/*.mp3
podcast/**/*.wav
# Design-scratch media (mockup screenshots, sample posters)
scratch/**/*.png
scratch/**/*.jpg
scratch/**/*.jpeg
scratch/**/*.mp4
# Excel lock files
~$*

