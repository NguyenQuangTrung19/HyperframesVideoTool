---
name: create-analysis-video
description: Generate a Vietnamese 9:16 motion-graphic football analysis video from a topic, URL, or .txt file. Supports match analysis, list/ranking ("Top 10 ..."), VS comparisons, history/career, and trivia content.
---

# Create Analysis Video Skill

Generate a Vietnamese 9:16 motion-graphic football **analysis** video.

This is the long-form sibling of `/create-news-video`:
- News: 5–8 scenes, ~60s, fast headline-style.
- Analysis: 10–15 scenes, **90–180s**, depth + storytelling.

## Input

Single argument — one of:
- **Topic** (Vietnamese, plain text): `"Top 10 Vua phá lưới Châu Âu mùa 2025-26"`, `"Messi vs Ronaldo: Ai vĩ đại hơn"`
- **URL** (`http://` or `https://`): article you want to expand into analysis
- **File** (`.txt`): your own notes / source material

## Content types this skill handles

| Type | Example topic | Dominant templates |
|---|---|---|
| Match analysis | "Phân tích chiến thuật Arsenal vs Real Madrid CK C1" | callout, stat-hero, comparison |
| List / Ranking | "Top 10 Vua phá lưới Châu Âu", "5 chuyển nhượng đắt nhất 2026" | stat-hero (per item), callout |
| VS comparison | "Messi vs Ronaldo: Ai vĩ đại hơn?" | comparison (heavy), stat-hero |
| History / Career | "Hành trình Modric: Từ chiến tranh đến QBV", "Cầu thủ còn thi đấu từ World Cup 2006" | callout, stat-hero, feature-list |
| Trivia / Did-you-know | "5 kỷ lục lạ lùng nhất Premier League" | callout, stat-hero |

## Plan-mode vs free-form mode

Before doing anything else, check if the source .txt has a sibling `images-plan.json` (i.e. `dirname(txtPath)/images-plan.json` exists). The presence of that file unlocks **plan mode**:

| | Plan mode (preferred for analysis) | Free-form mode (fallback) |
|---|---|---|
| Trigger | `images-plan.json` exists next to source .txt | No plan file — typical for old layouts |
| Output dir | `output/<slug>/` (no timestamp) | `output/<slug>-<timestamp>/` |
| sceneIds | **MUST match** plan's `scenes[].id` exactly | Free-choice (`hook`, `rank-1`, …) |
| imagePrompt | Copy plan's `prompt` verbatim into the matching scene | Generated by Claude |
| Image source | Pre-staged from input folder via `npm run images:stage` | Generated at pipeline runtime by Gemini/OpenAI/xAI |
| If image missing | **Halt** — `npm run images:stage` exits non-zero, report missing files to user | Pipeline falls back to gradient |

User is on the visual-first track when they ran `/images-for-videos` first. Respect their plan — do not reshuffle scenes, drop scenes the plan declared, or invent new image-eligible scenes the plan didn't anticipate. The plan is the contract.

When in plan mode, you may STILL add non-image-eligible scenes that the plan didn't include (e.g., a `feature-list` summary, a `comparison` card) — those don't need plan entries. The plan only governs scenes whose template is `hook` / `stat-hero` / `callout`.

## Workflow (MUST follow these steps in order)

### Step 1: Detect input type

- Starts with `http://` or `https://` → URL mode
- Path ends with `.txt` and exists → file mode
- Otherwise → **topic mode** (use Claude knowledge + optional WebSearch for recent stats)

### Step 2: Gather source material

**Topic mode:**
- Use Claude's knowledge for established facts (player careers, historic matches, etc.)
- For RECENT data (current season stats, latest transfers) → use WebSearch
- If you're not confident in a key stat (e.g., goal count), say so in the script with a softer phrasing ("hơn N bàn") rather than fabricate

**URL mode:** Use `WebFetch`, then classify the article body using the rules in `classify-football-content` skill.

**File mode (most common for analysis):**

⚠️ **First: classify the content using the [`classify-football-content`](../classify-football-content/SKILL.md) skill.** It defines 8 canonical types (RANKING, VS, MATCH ANALYSIS, PRE-MATCH PREVIEW, PLAYER PROFILE, HISTORY/CAREER, TRANSFER NEWS, TRIVIA), each with detection signals (filename + body cues), proposed scene structure, voice tone, and hook patterns. Read it before structuring the script — it's the single source of truth for "what shape should this video take".

After classification:

1. **`Read` the entire file.** Don't truncate — analysis files often contain many stats and context you'll need.
2. **Apply the type's structure** from the classifier (scene count, template sequence, voice tone). Don't invent a custom structure unless the file clearly doesn't fit any type.
3. **Extract facts:** pull out every concrete number, date, name, achievement. These become `stat-hero` values, `comparison` values, or `callout` claims.
4. **Restructure for spoken video:** the file content is written for reading, but the video is heard. Rewrite for spoken Vietnamese:
   - Break long sentences into short ones (1–2 sentences per scene voiceText).
   - Convert formal text to văn nói (spoken style).
   - Apply phonetic rules (numbers spelled out — see news skill TTS rules).
   - Drop tangential paragraphs that don't fit a 90–180s video.
5. **Polish & fact-check sparingly:** if the user's file states something you know to be wrong (e.g. wrong score, wrong year), fix it silently and proceed. If a key claim is unverifiable, soften the wording rather than echoing as definitive.
6. **Set metadata:**
   - `title` = best summary (max 80 chars). Derive from filename + content if no explicit title.
   - `source.url` = `"local"`, `source.domain` = `"local"`, `source.image` = `null`.
   - Hook scene gets `imagePrompt` (no og:image fallback in file mode).

### Step 3: Create slug + output directory

**Plan mode** (images-plan.json exists next to source .txt):
- slug = parent folder name of the .txt (e.g. `input/topCBsITW/topCBsITW.txt` → slug = `topCBsITW`). Falls back to file stem when txt isn't in a subfolder.
- outputDir = `output/<slug>/` — **no timestamp suffix**, reruns overwrite for idempotency.
- `mkdir -p <outputDir>`

**Free-form mode** (no plan file):
- slug = lowercase ASCII (strip Vietnamese diacritics, đ→d), replace non-alphanumeric with `-`, max 50 chars (analysis topics are longer than news)
- timestamp = current local time as `YYYYMMDD-HHmm`
- outputDir = `output/<slug>-<timestamp>/`
- `mkdir -p <outputDir>`

### Step 4: Generate script.json

**Schema:** same as news skill (see `src/render/script-schema.ts`). Key differences:

| | News | Analysis |
|---|---|---|
| Scene count | 5–8 | **10–15** |
| Total words | 150–200 | **400–600** |
| Words/scene | 25–40 | **30–50** |
| Total duration | 55–65s | **90–180s** |
| `imagePrompt` | Optional | **Strongly recommended on every hook/callout/stat-hero** |

**Vietnamese TTS phonetic rules — same as news skill.** Spell out numbers in Vietnamese phonetic form in `voiceText`. See `/create-news-video` SKILL.md for the full table — those rules apply identically here.

**Hook (most important — first 3s of viewer attention):**
- Opens with a CLAIM or surprising fact, not a generic intro
- Examples:
  - LIST: `"Năm cầu thủ này đã thi đấu liên tục từ World Cup hai nghìn không trăm linh sáu đến nay — bạn có nhận ra ai không?"`
  - VS: `"Messi và Ronaldo: ai mới thực sự là số một? Số liệu sẽ trả lời."`
  - HISTORY: `"Từ một cậu bé tị nạn chiến tranh, Modric đã trở thành biểu tượng của bóng đá hiện đại."`

**Body — content-type specific patterns:**

The full taxonomy of 8 content types (RANKING, VS, MATCH ANALYSIS, PRE-MATCH PREVIEW, PLAYER PROFILE, HISTORY/CAREER, TRANSFER NEWS, TRIVIA) — including detection signals, scene structure, template sequence, voice tone, and hook patterns — lives in [`classify-football-content`](../classify-football-content/SKILL.md). Use it as your single source of truth.

The one inline rule kept here because it's enforced at script-write time:

**⚠️ stat-hero.value rule for RANKING content — pick ONE of two patterns:**

| | When to use | `value` content | Example |
|---|---|---|---|
| **A1. Metric-driven** | Ranking IS a clear number (top scorers by goals, fastest goals, most expensive transfers) | The metric itself | `"41"` (goals), `"€222M"` (fee) |
| **A2. Editorial** | Ranking is a JUDGMENT (best CBs, greatest moments, most influential) | The rank itself | `"#7"`, `"#6"`, ..., `"#1"` |

For A2, **never** put random secondary stats in `value` (age, height, transfer fee) — they confuse viewers because the number doesn't connect to the rank position. Put those in `context` instead.

**Outro (always fixed format):**
```json
{
  "id": "outro",
  "type": "outro",
  "voiceText": "Theo dõi Bóng lăn để xem nhiều phân tích sâu hơn mỗi tuần.",
  "templateData": {
    "template": "outro",
    "ctaTop": "Theo dõi ngay",
    "channelName": "Bóng lăn",
    "source": "Sưu tầm"
  }
}
```
The video renders `
: ${source}` at the end. **Always use `"Sưu tầm"`** regardless of input mode. Analysis content is editorially curated, not direct attribution to a single article.

### imagePrompt rules (CRITICAL for analysis videos)

Analysis videos live or die by visuals. Every hook/callout/stat-hero scene SHOULD have an `imagePrompt`. Same rules as news skill:
- **English** prompts (GPT-Image is much stronger in English)
- Sports photography style — cinematic, action, atmospheric
- Include `"vertical 9:16 composition"` or `"portrait orientation"`
- 30–100 words
- Avoid asking for text/scoreboards/logos in the image (your captions already cover that)
- For specific players, describe physical traits + kit color rather than naming the club crest (model may refuse club logos)

**Example imagePrompts for football contexts:**

| Context | Prompt |
|---|---|
| Player goal (Haaland) | `Cinematic action photo of a tall blond Norwegian football striker in light blue kit celebrating a goal at a packed European stadium, dramatic floodlights, intense expression, vertical 9:16 portrait, sports photography, photo-realistic` |
| Coach on touchline | `Football coach in dark suit on touchline at modern European stadium, intense focused expression, gesturing toward pitch, evening match lighting, blurred crowd in background, vertical 9:16, sports photography, dramatic mood` |
| Stadium atmosphere | `Wide low-angle shot of a European football stadium at night, packed stands, floodlights piercing fog, dramatic atmosphere, vertical 9:16 composition, cinematic sports photography` |
| Trophy lift | `Football team captain lifting a large silver trophy above his head on a stadium pitch, gold confetti raining, teammates celebrating in background, vertical 9:16, golden hour lighting, photo-realistic, cinematic sports photography` |
| Penalty kick moment | `Football player in red kit about to take a penalty kick at a packed European stadium, goalkeeper crouched in dark green kit, tense atmosphere, vertical 9:16 portrait, dramatic floodlights, photo-realistic sports photography` |

### Step 5: Self-validate before writing

- Total word count ~400–600
- 10–15 scenes total
- scenes[0].type === "hook"
- last scene type === "outro"
- Each text field ≤ schema max (use the field caps in `script-schema.ts` — e.g., `headline ≤ 40`, `bullets[i] ≤ 50`)
- Every hook/callout/stat-hero has an `imagePrompt` — fix yourself silently if missing

### Step 6: Write script.json

Use the `Write` tool to write to `<outputDir>/script.json`.

**Plan mode extra rule:** every plan scene must appear in script.json with the EXACT same `id` and a matching `templateData.template`. Mismatched ids = staged image won't bind. `imagePrompt` field should be copied verbatim from the plan (it acts as a fallback if the staged image is later removed).

### Step 7 (plan mode only): Stage planned images

If `images-plan.json` exists alongside the source .txt, run:

```bash
npm run images:stage -- <pathToSourceTxt>
```

The script:
1. Reads `images-plan.json`
2. Verifies every planned `filename` exists in the input folder
3. **Exits non-zero** with a list of missing files (and their prompts) if any are absent
4. Otherwise copies each planned file to `output/<slug>/images/<sceneId>.<ext>` so the pipeline's override mechanism picks them up

If the staging step fails:
- DO NOT proceed to the pipeline.
- Surface the missing-files list verbatim to the user.
- Tell them to generate the missing images on grok.com using the listed prompts and re-run `/create-analysis-video`.

### Step 8: Run the pipeline

```bash
npm run pipeline -- <outputDir>/script.json
```

**Expected latency:** TTS ~30–60s + image gen ~30–90s (parallel) + render ~3–5min. Analysis videos take longer than news due to more scenes.

In plan mode the image generation step is essentially free — every eligible scene already has a staged file at `output/<slug>/images/<sceneId>.<ext>`, so the pipeline logs `MANUAL OVERRIDE` for each and skips the AI provider entirely.

### Step 9: Report success

```markdown
✓ Video:  [video.mp4](<outputDir>/video.mp4)
✓ Audio:  [voice.mp3](<outputDir>/voice.mp3) — for CapCut
✓ Script: [script.txt](<outputDir>/script.txt) — for CapCut auto-caption
Tổng thời lượng: XX.Xs
```

(Use the actual output dir — `output/<slug>/` in plan mode, `output/<slug>-<timestamp>/` in free-form mode.)

## Examples

### Example 1: List / Ranking — "Top 5 Vua phá lưới C1 mùa 2024-25"

```json
{
  "version": "1.0",
  "metadata": {
    "title": "Top 5 Vua phá lưới Champions League mùa 2024-25",
    "source": { "url": "local", "domain": "local", "image": null },
    "channel": "Bóng lăn"
  },
  "voice": { "provider": "lucylab", "voiceId": "${VIETNAMESE_VOICEID}", "speed": 1.0 },
  "scenes": [
    {
      "id": "hook", "type": "hook",
      "voiceText": "Năm cây săn bàn đáng sợ nhất Champions League mùa hai nghìn không trăm hai mươi bốn — ai đứng đầu sẽ khiến bạn bất ngờ.",
      "templateData": { "template": "hook", "headline": "Top 5 Vua phá lưới", "subhead": "Champions League 24-25", "kenBurns": "zoom-in" },
      "imagePrompt": "Cinematic wide-angle photo of a packed European football stadium at night, golden Champions League starball logo glowing on scoreboard, dramatic floodlights, fog, vertical 9:16 composition, photo-realistic sports photography"
    },
    {
      "id": "rank-5", "type": "body",
      "voiceText": "Hạng năm, Vinicius Junior — chín bàn cùng năm pha kiến tạo, tốc độ và kỹ thuật của anh vẫn là cơn ác mộng cho mọi hàng thủ.",
      "templateData": { "template": "stat-hero", "value": "9", "label": "Vinicius Jr.", "context": "Bàn thắng + 5 kiến tạo" },
      "imagePrompt": "Cinematic action photo of a young Brazilian football winger in white kit dribbling at high speed past a defender at a packed European stadium, dramatic floodlights, intense focus, vertical 9:16 portrait, photo-realistic, sports photography"
    },
    {
      "id": "rank-4", "type": "body",
      "voiceText": "Hạng tư, Robert Lewandowski — mười bàn cho Barcelona, một tay đưa đội bóng vào bán kết.",
      "templateData": { "template": "stat-hero", "value": "10", "label": "R. Lewandowski", "context": "Bàn thắng / 11 trận" },
      "imagePrompt": "Cinematic action photo of an experienced Polish football striker in dark blue and red striped kit celebrating a goal with arms outstretched, dramatic stadium lighting, intense expression, vertical 9:16 portrait, photo-realistic, sports photography"
    },
    {
      "id": "rank-3", "type": "body",
      "voiceText": "Hạng ba, Kylian Mbappé — mười một bàn trong mùa đầu khoác áo Real Madrid, tốc độ vẫn không có đối thủ.",
      "templateData": { "template": "stat-hero", "value": "11", "label": "Kylian Mbappé", "context": "Mùa đầu tại Real" },
      "imagePrompt": "Cinematic action photo of a French football striker in white Real Madrid kit sprinting at full speed with the ball at a packed Santiago Bernabéu, dramatic floodlights, intense focus, vertical 9:16 portrait, photo-realistic, sports photography"
    },
    {
      "id": "rank-2", "type": "body",
      "voiceText": "Hạng nhì, Erling Haaland — mười ba bàn cho Manchester City, hiệu suất gần như mỗi trận một bàn.",
      "templateData": { "template": "stat-hero", "value": "13", "label": "Erling Haaland", "context": "Hơn 1 bàn / trận" },
      "imagePrompt": "Cinematic action photo of a tall blond Norwegian football striker in light blue Manchester City kit roaring after scoring a goal at Etihad Stadium, dramatic floodlights, intense expression, vertical 9:16 portrait, photo-realistic, sports photography"
    },
    {
      "id": "rank-1-tease", "type": "body",
      "voiceText": "Và hạng nhất sẽ khiến nhiều người bất ngờ — không phải Mbappé, không phải Haaland.",
      "templateData": { "template": "callout", "statement": "Hạng nhất không phải tên bạn nghĩ.", "tag": "Twist" },
      "imagePrompt": "Wide cinematic shot of a European football stadium at night, dramatic stadium floodlights creating dramatic shadows, fog rolling across the pitch, anticipation atmosphere, vertical 9:16 composition, photo-realistic sports photography"
    },
    {
      "id": "rank-1", "type": "body",
      "voiceText": "Hạng nhất, Raphinha — mười lăm bàn thắng cùng tám pha kiến tạo, người đã khoác lên Barcelona một sức sống mới.",
      "templateData": { "template": "stat-hero", "value": "15 + 8", "label": "Raphinha", "context": "Bàn thắng + kiến tạo" },
      "imagePrompt": "Cinematic action photo of a Brazilian football winger in dark blue and red striped Barcelona kit celebrating after scoring, arms wide, intense joy expression, packed stadium background, dramatic floodlights, vertical 9:16 portrait, photo-realistic, sports photography"
    },
    {
      "id": "context", "type": "body",
      "voiceText": "Một mùa giải mà các tiền đạo trẻ thực sự thống trị. Bốn trong năm cầu thủ này đều dưới hai mươi sáu tuổi.",
      "templateData": { "template": "callout", "statement": "Thế hệ vàng mới đã đến.", "tag": "Insight" },
      "imagePrompt": "Cinematic group photo composition of multiple young football players in different European club kits standing side by side on a stadium pitch at night, heroic lighting, vertical 9:16 composition, photo-realistic sports photography"
    },
    {
      "id": "outro", "type": "outro",
      "voiceText": "Theo dõi Bóng lăn để xem nhiều phân tích sâu hơn mỗi tuần.",
      "templateData": { "template": "outro", "ctaTop": "Theo dõi ngay", "channelName": "Bóng lăn", "source": "Sưu tầm" }
    }
  ]
}
```

### Example 2: File mode — VS comparison from `RonaldoOrMessi.txt`

User: `/create-analysis-video RonaldoOrMessi.txt`

**Sample input file** (free-form notes, what a user typically writes):
```
Messi vs Ronaldo - ai vĩ đại hơn?

Messi:
- 8 Quả Bóng Vàng
- 821 bàn thắng sự nghiệp
- Vô địch World Cup 2022
- 44 danh hiệu chính thức
- Sút phạt: 65+ bàn
- Cao 1m70

Ronaldo:
- 5 Quả Bóng Vàng
- 924 bàn thắng sự nghiệp
- Vô địch Euro 2016 + Nations League 2019
- 35 danh hiệu chính thức
- Sút phạt: 60+ bàn
- Cao 1m87

Đỉnh cao: Messi tinh tế, Ronaldo bùng nổ.
Cả hai đều đã định nghĩa lại bóng đá hiện đại, mỗi người một cách.
```

**Skill workflow:**
1. `Read` the file → identify VS content from filename + structure.
2. Extract facts: 2 lists of stats per player.
3. Restructure: hook (claim) → 5–7 comparison scenes (one per metric) → final callout (verdict) → outro.
4. Apply phonetic rules: "8 Quả Bóng Vàng" → voiceText `"tám Quả Bóng Vàng"`; "1m70" → `"một mét bảy mươi"`; "821 bàn" → `"tám trăm hai mươi mốt bàn"`.

**Generated `script.json` (excerpt):**
```json
{
  "version": "1.0",
  "metadata": {
    "title": "Messi vs Ronaldo: Ai mới thực sự vĩ đại hơn?",
    "source": { "url": "local", "domain": "local", "image": null },
    "channel": "Bóng lăn"
  },
  "voice": { "provider": "lucylab", "voiceId": "${VIETNAMESE_VOICEID}", "speed": 1.0 },
  "scenes": [
    {
      "id": "hook", "type": "hook",
      "voiceText": "Messi và Ronaldo: ai mới thực sự vĩ đại hơn? Số liệu sẽ trả lời.",
      "templateData": { "template": "hook", "headline": "Messi vs Ronaldo", "subhead": "Số liệu lên tiếng", "kenBurns": "zoom-in" },
      "imagePrompt": "Cinematic split-frame composition with two football legends side by side at night stadium, one in light blue and white striped kit on left, another in white kit on right, dramatic stadium floodlights, golden trophy lighting, vertical 9:16 portrait composition, photo-realistic sports photography"
    },
    {
      "id": "compare-balon-dor", "type": "body",
      "voiceText": "Quả Bóng Vàng — Messi tám lần, Ronaldo năm lần. Lợi thế nghiêng về số mười.",
      "templateData": {
        "template": "comparison",
        "left":  { "label": "Messi",   "value": "8 QBV", "color": "cyan" },
        "right": { "label": "Ronaldo", "value": "5 QBV", "color": "purple", "winner": false }
      },
      "imagePrompt": "Cinematic photo of a small Argentine football player in light blue and white striped kit holding a golden Ballon d'Or trophy, dramatic spotlight, vertical 9:16 portrait, photo-realistic sports photography"
    },
    {
      "id": "compare-goals", "type": "body",
      "voiceText": "Tổng số bàn thắng — Ronaldo dẫn trước với chín trăm hai mươi bốn, Messi tám trăm hai mươi mốt.",
      "templateData": {
        "template": "comparison",
        "left":  { "label": "Messi",   "value": "821 bàn", "color": "cyan" },
        "right": { "label": "Ronaldo", "value": "924 bàn", "color": "purple", "winner": true }
      },
      "imagePrompt": "Cinematic action photo of a Portuguese football striker in white Real Madrid kit celebrating a goal with iconic siu jump, dramatic stadium lighting, vertical 9:16 portrait, photo-realistic sports photography"
    },
    {
      "id": "compare-trophies", "type": "body",
      "voiceText": "Danh hiệu chính thức — Messi bốn mươi bốn, Ronaldo ba mươi lăm. Số mười áp đảo về sưu tập.",
      "templateData": {
        "template": "comparison",
        "left":  { "label": "Messi",   "value": "44 cúp", "color": "cyan", "winner": true },
        "right": { "label": "Ronaldo", "value": "35 cúp", "color": "purple" }
      }
    },
    {
      "id": "compare-international", "type": "body",
      "voiceText": "Đấu trường quốc tế — Messi vô địch World Cup hai nghìn không trăm hai mươi hai, Ronaldo có Euro hai nghìn không trăm mười sáu và Nations League.",
      "templateData": {
        "template": "comparison",
        "left":  { "label": "Messi",   "value": "World Cup",  "color": "cyan", "winner": true },
        "right": { "label": "Ronaldo", "value": "Euro + NL",  "color": "purple" }
      },
      "imagePrompt": "Cinematic photo of a small Argentine football player in light blue and white striped national team kit lifting the golden World Cup trophy at a Middle Eastern stadium, dramatic celebration, vertical 9:16 portrait, photo-realistic sports photography"
    },
    {
      "id": "compare-style", "type": "body",
      "voiceText": "Phong cách — Messi tinh tế, không gian, kỹ thuật cá nhân. Ronaldo bùng nổ, thể chất, không chiến.",
      "templateData": { "template": "callout", "statement": "Messi tinh tế. Ronaldo bùng nổ.", "tag": "Phong cách" },
      "imagePrompt": "Cinematic split composition showing two contrasting football moments — left side a player dribbling with delicate footwork, right side a player jumping high for a header, dramatic stadium lighting, vertical 9:16 portrait, photo-realistic sports photography"
    },
    {
      "id": "compare-physical", "type": "body",
      "voiceText": "Về thể hình — Messi cao một mét bảy mươi, Ronaldo một mét tám mươi bảy. Người ta tài năng theo những cách rất khác nhau.",
      "templateData": {
        "template": "comparison",
        "left":  { "label": "Messi",   "value": "1m70", "color": "cyan" },
        "right": { "label": "Ronaldo", "value": "1m87", "color": "purple" }
      }
    },
    {
      "id": "verdict", "type": "body",
      "voiceText": "Câu trả lời thật ra không có. Cả hai đã định nghĩa lại bóng đá hiện đại, mỗi người một cách. Chúng ta may mắn vì được sống cùng thời đại của họ.",
      "templateData": { "template": "callout", "statement": "Cả hai đều là huyền thoại — theo cách của riêng mình.", "tag": "Verdict" },
      "imagePrompt": "Cinematic photo of two football legends standing together on a stadium pitch at golden hour, mutual respect atmosphere, packed stadium in background, vertical 9:16 portrait, photo-realistic sports photography"
    },
    {
      "id": "outro", "type": "outro",
      "voiceText": "Theo dõi Bóng lăn để xem nhiều phân tích sâu hơn mỗi tuần.",
      "templateData": { "template": "outro", "ctaTop": "Theo dõi ngay", "channelName": "Bóng lăn", "source": "Sưu tầm" }
    }
  ]
}
```

**Notes about the transformation:**
- Source file used `8 QBV` / `1m70` / `821 bàn` → voiceText spells out `"tám Quả Bóng Vàng"` / `"một mét bảy mươi"` / `"tám trăm hai mươi mốt bàn"` per phonetic rules. The visual `templateData.value` keeps the compact original (`"8 QBV"`, `"1m70"`, `"821 bàn"`).
- 9 scenes total (1 hook + 7 body + 1 outro). Within the 5–16 schema cap and the 10–15 analysis target window.
- `winner: true` is set per metric on whichever player leads.
- The user's note "ai vĩ đại hơn" gets a balanced verdict scene rather than a forced winner — preserves the user's nuanced ending.

### Example 3: History / Career — "Cầu thủ còn thi đấu từ World Cup 2006"

Structure: hook → 1 scene per remaining player (callout with story + stat-hero with key number) → final reflection callout → outro.

Each player scene benefits from imagePrompt featuring the player's signature look (e.g., "veteran Spanish midfielder in dark suit watching from technical area").

## Edge cases

| Situation | Action |
|---|---|
| Topic too narrow ("Top 1") | Suggest user broaden to top 5/10 — analysis needs depth |
| Topic too broad ("History of football") | Ask user to narrow scope to specific era / competition / league |
| Stats not in Claude knowledge + no internet | Tell user: "Cần số liệu cập nhật, hãy cung cấp file .txt với thông tin nguồn" — don't fabricate |
| URL paywalled | Same as news skill — ask for .txt |
| Total duration < 80s after generation | Add 1–2 more body scenes |
| Total duration > 200s | Cut redundant scenes or shorten voiceText |

## Channel context

This skill writes for the **Bóng lăn** (`@bonglan0702`) channel — Vietnamese football news + analysis. Outro and metadata.channel are both `"Bóng lăn"`. See `memory/project_channel_focus.md` for full brand context.
